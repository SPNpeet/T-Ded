# แจกให้คนอื่นใช้ และเก็บค่าบริการ

## สถานะตอนนี้

| ส่วน | พร้อมหรือยัง |
|---|---|
| แอป Android (APK แจกตรง) | พร้อม https://github.com/SPNpeet/T-Ded/releases/latest |
| แอป iOS | โปรเจกต์พร้อม build บน Mac (ดู APP.md) |
| เว็บใช้ฟรี (เครื่องคำนวณ) | พร้อม https://spnpeet.github.io/T-Ded/ |
| ระบบสมัครเอง แยกข้อมูลรายฟาร์ม | พร้อม (สมัครแล้วได้ทดลองฟรี 60 วันอัตโนมัติ) |
| ระบบแพ็กเกจ จำกัดสิทธิ์ ต่ออายุ รับเงิน | พร้อม (ทดสอบครบวงจรแล้ว) |
| นโยบายความเป็นส่วนตัว / เงื่อนไขการใช้งาน | พร้อม (ในแอปและเปิดจากเว็บได้) |
| **เซิร์ฟเวอร์ถาวร** | **ยังไม่มี — นี่คือสิ่งเดียวที่ค้างอยู่** |

## ทำไมต้องมีเซิร์ฟเวอร์ถาวรก่อนแจก

ตอนนี้แอปต้องให้ผู้ใช้ "ใส่ที่อยู่เซิร์ฟเวอร์" เอง และลิงก์จาก `start-mobile.cmd` เปลี่ยนทุกครั้งที่เปิดใหม่ ถ้าแจกให้เกษตรกร 10 คนแล้วลิงก์เปลี่ยน ทุกคนจะใช้ไม่ได้พร้อมกัน

เมื่อมีเซิร์ฟเวอร์ถาวรแล้ว ผมจะฝังที่อยู่ลงในแอปตอน build (ตัวแปร `VITE_API_BASE`) ผู้ใช้จะ **เปิดแอปแล้วสมัครได้เลย ไม่ต้องตั้งค่าอะไร**

**ทางที่ถูกที่สุด:** Oracle Cloud Always Free (ฟรีถาวร เครื่อง ARM 4 คอร์ 24GB ที่สิงคโปร์) ขั้นตอนอยู่ใน DEPLOY.md

## แพ็กเกจที่ตั้งไว้ (แก้ราคาได้ทุกเมื่อ)

| แพ็กเกจ | ราคา/เดือน | ฟาร์ม | บ่อ | ผู้ใช้ |
|---|---|---|---|---|
| ทดลองใช้ (อัตโนมัติเมื่อสมัคร 60 วัน) | ฟรี | 1 | 5 | 3 |
| ฟรี (หลังหมดทดลอง ถ้าไม่ต่อ) | ฟรี | 1 | 2 | 2 |
| มาตรฐาน | 199 | 1 | 10 | 5 |
| มืออาชีพ | 590 | 5 | 50 | 20 |
| ไม่จำกัด (หน่วยงานส่งเสริม) | ตกลงราคา | 999 | 9999 | 999 |

แก้ราคา/สิทธิ์ได้ที่ `crates/server/src/billing.rs` ฟังก์ชัน `plan_defaults` หรือกำหนดรายลูกค้าจากหน้าแอดมิน

## วิธีเก็บเงินตอนนี้ (แบบไม่ต้องต่อระบบชำระเงิน)

1. ลูกค้าโอน/พร้อมเพย์มาที่บัญชีของพี่
2. เข้าแอปด้วยบัญชีแอดมิน > **ฟาร์มทั้งหมด > แท็บ ลูกค้า/รายได้**
3. หาชื่อลูกค้า > กด **รับเงิน** > ใส่จำนวนเงินและจำนวนเดือน > บันทึก
4. ระบบต่ออายุให้ทันที ลูกค้าใช้ได้ต่อโดยไม่ต้องติดตั้งอะไรใหม่

หน้าเดียวกันนี้แสดง **รายได้รวม รายได้เดือนนี้ MRR จำนวนลูกค้าที่ใช้งานอยู่ และลูกค้าที่จ่ายเงิน**

## เมื่อโตขึ้นค่อยทำ

- ต่อพร้อมเพย์อัตโนมัติ (ตรวจสลิป/Payment Gateway) แทนการกดรับเงินเอง
- ขึ้น Play Store (25 USD ครั้งเดียว) และ App Store (99 USD/ปี) — ต้องใช้ลิงก์นโยบายความเป็นส่วนตัวที่ทำไว้แล้ว: `https://spnpeet.github.io/T-Ded/#/privacy`
- ออกใบเสร็จ/ใบกำกับภาษีถ้าจดทะเบียน
- สำรองข้อมูลอัตโนมัติและแผน PDPA ตามที่ระบุใน SECURITY.md (ควรทำก่อนรับลูกค้าจริงจำนวนมาก)

## สิ่งที่ต้องทำก่อนรับลูกค้าจริง

1. ตั้งเซิร์ฟเวอร์ถาวร (ครั้งเดียว ~15 นาที)
2. เปลี่ยน PIN บัญชีแอดมินและลบบัญชีทดสอบ
3. ตั้งสำรองข้อมูลรายวันของไฟล์ `data/teedet.db`
4. ใส่ชื่อผู้ติดต่อจริงในหน้านโยบายความเป็นส่วนตัว (ตอนนี้เขียนว่า "ผู้ดูแลระบบทีเด็ดปลาน้ำจืด")
