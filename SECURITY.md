# ความปลอดภัยของระบบ (อ้างอิง ISO/IEC 27001:2022)

เอกสารนี้บอกตามจริงว่า **อะไรทำแล้ว** และ **อะไรยังต้องทำ** ก่อนจะอ้างได้ว่าสอดคล้องมาตรฐาน

> ข้อเท็จจริงสำคัญ: ISO 27001 เป็นการรับรอง **องค์กร** ไม่ใช่การรับรองซอฟต์แวร์ ซอฟต์แวร์ทำได้แค่รองรับมาตรการทางเทคนิค (Annex A) ส่วนที่เหลือเป็นเรื่องนโยบาย เอกสาร และการตรวจสอบขององค์กร

## มาตรการที่ทำแล้วในระบบ (ตรวจสอบด้วยการยิงจริงแล้ว)

| Annex A | มาตรการ | ทำอย่างไรในระบบนี้ | ไฟล์ |
|---|---|---|---|
| A.5.15 ควบคุมการเข้าถึง | แบ่งสิทธิ์ 4 บทบาท: เจ้าของฟาร์ม / คนงาน / เจ้าหน้าที่ส่งเสริม / แอดมิน และตรวจสิทธิ์ทุก endpoint ที่แตะข้อมูลฟาร์ม | `crates/server/src/api.rs` (assert_farm_access) |
| A.5.17 ข้อมูลยืนยันตัวตน | PIN เก็บแบบ **argon2 hash + salt** ไม่เก็บ PIN จริง และห้ามตั้ง PIN ที่เดาง่าย (1234, 0000, เลขซ้ำ, เลขเรียง) | `crates/server/src/auth.rs`, `security.rs` |
| A.8.5 การยืนยันตัวตนที่ปลอดภัย | กรอกผิดเกิน 5 ครั้ง ล็อก 15 นาที (นับแยกรายเบอร์) · session หมดอายุถ้าไม่ใช้งาน 30 วัน และหมดอายุถาวรที่ 90 วัน · ลบ session หมดอายุอัตโนมัติทุกชั่วโมง | `security.rs`, `auth.rs`, `main.rs` |
| A.8.15 การบันทึกล็อก | บันทึกทุกการเข้าสู่ระบบ (สำเร็จ/ล้มเหลว/ถูกล็อก) และทุกการแก้ข้อมูลสำคัญลงตาราง `audit_log` โดย **เก็บเบอร์แค่ 4 ตัวท้าย** เพื่อลดข้อมูลส่วนบุคคล | `auth.rs`, `api.rs` |
| A.8.16 การเฝ้าระวัง | หน้าแอดมินดู audit log ย้อนหลังได้ | `web/src/pages/Admin.svelte` |
| A.8.23 การกรองเว็บ | ส่ง security headers ครบ: CSP, X-Frame-Options DENY, X-Content-Type-Options, HSTS, Referrer-Policy, Permissions-Policy | `security.rs` |
| A.8.24 การเข้ารหัส | ทุกการเชื่อมต่อผ่าน HTTPS (Cloudflare tunnel / โฮสต์) และ HSTS บังคับฝั่งเบราว์เซอร์ | `security.rs` |
| A.8.28 การเขียนโค้ดปลอดภัย | ใช้ Rust (ปลอดภัยด้านหน่วยความจำ) และ query ผ่าน parameter binding ทั้งหมด กัน SQL injection | ทั้งโปรเจกต์ |
| A.5.34 ความเป็นส่วนตัว | ข้อมูลโรคที่แชร์สาธารณะตัดชื่อฟาร์มออก และปัดพิกัดเหลือ ~10 กม. · repo สาธารณะไม่มีข้อมูลลูกค้า | `api.rs` (list_disease_reports) |

**ยืนยันการทำงานจริง (ทดสอบเมื่อ 15 ส.ค. 2569):** PIN 1234 ถูกปฏิเสธ · กรอกผิด 5 ครั้งแล้วถูกล็อก 15 นาที และ PIN ที่ถูกต้องก็ยังเข้าไม่ได้ระหว่างล็อก · security headers ครบทุกตัว

## ยังไม่ได้ทำ — ต้องทำก่อนใช้กับข้อมูลจริงหลายฟาร์ม

| เรื่อง | ทำไมจำเป็น | ทางแก้ |
|---|---|---|
| **สำรองข้อมูลอัตโนมัติ** (A.8.13) | ตอนนี้ข้อมูลอยู่ในไฟล์ SQLite ไฟล์เดียว ถ้าเครื่องพัง = ข้อมูลหาย | ตั้ง cron คัดลอก `data/teedet.db` ขึ้นที่เก็บภายนอกทุกวัน + ทดสอบกู้คืนจริง |
| **เข้ารหัสข้อมูลขณะพัก** (A.8.24) | ถ้ามีคนเข้าถึงเครื่องได้ จะอ่านไฟล์ฐานข้อมูลได้ทันที | เปิด disk encryption ที่เครื่อง/VM หรือใช้ SQLCipher |
| **จำกัดอัตราเรียก API ทั้งระบบ** (A.8.6) | ตอนนี้กันเฉพาะหน้าล็อกอิน endpoint อื่นยังยิงถี่ได้ | เพิ่ม rate limit ต่อ IP ที่ระดับ proxy (Cloudflare) หรือ tower middleware |
| **ทบทวนสิทธิ์ผู้ใช้ตามรอบ** (A.5.18) | คนงานที่ออกไปแล้วอาจยังเข้าได้ | นัดทบทวนรายชื่อผู้ใช้ทุก 3 เดือน (ระบบมีหน้ารายชื่อผู้ใช้แล้ว แต่ยังไม่มีปุ่มปิดบัญชี) |
| **แผนรับมือเหตุการณ์** (A.5.24-A.5.28) | ต้องรู้ว่าใครทำอะไรเมื่อข้อมูลรั่ว | เขียนขั้นตอน: ใครแจ้ง ใครปิดระบบ แจ้งผู้ได้รับผลกระทบภายในกี่ชั่วโมง |
| **PDPA** | ระบบเก็บชื่อ-เบอร์โทรเกษตรกร = ข้อมูลส่วนบุคคลตามกฎหมายไทย | ต้องมีนโยบายความเป็นส่วนตัว ขอความยินยอม และช่องทางขอลบข้อมูล |

## คำแนะนำสั้น ๆ สำหรับตอนนี้

1. เปลี่ยน PIN ของบัญชีแอดมินและบัญชีทดสอบทั้งหมดก่อนเปิดให้คนอื่นใช้
2. สำรองไฟล์ `data/teedet.db` ทุกวัน (คัดลอกไปไดรฟ์อื่น/คลาวด์ก็ยังดีกว่าไม่มี)
3. อย่าเปิดพอร์ต 8787 ตรงออกอินเทอร์เน็ต ให้ผ่าน HTTPS tunnel หรือโฮสต์ที่มี TLS เสมอ
4. ถ้าจะรับข้อมูลเกษตรกรจริงหลายฟาร์ม ให้ทำนโยบาย PDPA และการสำรองข้อมูลก่อน
